---
sidebar_position: 1
---

# 架构约束逻辑

每个受治理流程在本目录使用一个独立 Markdown 文件维护，文档导航由文件及其
`sidebar_position` 生成。不要在本页重复维护主题清单。

修改流程前，先更新对应主题文件中的范围、连线图、时序图、代码归属和必要不变量，
确认逻辑闭环后再改代码。新增流程时先创建中文主题文件，再同步英文文件。

详细产品说明继续放在原开发指南；本目录只保存可评审的架构真值。
