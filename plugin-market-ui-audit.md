# 插件市场 UI 一致性审计

## 审计范围

- 一级页：市场首页、筛选、搜索、已安装横条、展开列表、空状态
- 二级页：插件详情、开始使用、包含能力、信息区、管理页、创建页
- 菜单：创建菜单、市场卡片操作、详情操作、管理行操作
- 弹层：添加市场、安装、安装成功、卸载、技能详情、权限分享、插件上传、技能上传
- 状态：默认、Hover、Focus、禁用、加载、成功、错误、空状态

真实 Tauri 审计截图位于：

- 修改前：`wework/test-results/ai-verify/plugin-market-full-audit/before`
- 修改后：`wework/test-results/ai-verify/plugin-market-full-audit/after`
- 前后对照：`wework/test-results/ai-verify/plugin-market-full-audit/before-after-comparison.png`

## 归纳问题与处理

| 类别       | 审计发现                                                        | 统一结果                                                                         |
| ---------- | --------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 边框       | 页面、卡片、菜单和弹层混用全强度边框、`/35`、`/50` 与硬编码暖灰 | 容器默认使用 `border/30`，分隔线使用 `border/25`，Hover 最多提升至约 `/50`       |
| Hover      | 详情模板和技能能力缺少可辨认反馈；部分普通 Hover 使用品牌青色   | 普通可交互项统一使用中性 `muted/surface`，不再用品牌色表达普通 Hover             |
| Focus      | 多处写了 `focus-*` 类但设计令牌未注册，键盘焦点可能不生效       | 新增 `#339CFF` Focus 令牌并映射到 Tailwind，输入、按钮、卡片、菜单统一蓝色焦点环 |
| 弹层       | 遮罩约 35%–38%、阴影过重、圆角和宽度不一致                      | 遮罩统一为 13%，面板统一 20px 圆角、轻量阴影和弱边框；短确认弹层缩至 420px       |
| 菜单       | 44px 行高、粗体、小字号和重阴影混用                             | 菜单统一 36px 行高、14px 常规字重、弱边框和轻阴影                                |
| 字号与节奏 | 标题使用 `font-semibold`，正文行高和区块间距不稳定              | 统一到现有 18/14/13/12 字号层级，标题用 500 字重，正文用 20px 左右行高           |
| 图标       | 安装和技能弹层对已解析图标二次转换，导致公共资源路径失效        | 传递已解析资源，不再二次转换；详情、安装和技能弹层使用同一权威图标               |
| 语义颜色   | 普通交互、状态色和危险操作混用                                  | 中性交互使用灰阶；成功开关保留绿色；红色仅用于卸载等破坏性操作                   |

## 最终状态

- P0/P1/P2 一致性问题已处理。
- 权限分享弹层在隔离数据中没有“个人创建且可分享”的插件，无法从真实流程打开；代码已统一到同一弹层规范，但这是剩余的实机截图覆盖缺口。
- 旧的 MCP/Skill 目录组件当前没有从插件市场主导航暴露，不纳入本轮主路径视觉验收。
