diff --git a/core/util/browser.js b/core/util/browser.js
--- a/core/util/browser.js
+++ b/core/util/browser.js
@@ -81,9 +81,9 @@ async function _checkWebCodecsH264DecodeSupport() {

-    // We'll need to make do with some placeholders here
+    // Match the bundled 16x16 constrained-baseline probe frame.
     const config = {
-        codec: 'avc1.42401f',
-        codedWidth: 1920,
-        codedHeight: 1080,
+        codec: 'avc1.42c014',
+        codedWidth: 16,
+        codedHeight: 16,
         optimizeForLatency: true,
     };

diff --git a/core/rfb.js b/core/rfb.js
--- a/core/rfb.js
+++ b/core/rfb.js
@@ -26 +26 @@ import XtScancode from "./input/xtscancodes.js";
-import { encodings } from "./encodings.js";
+import { encodings, encodingName } from "./encodings.js";
@@ -153,0 +154 @@ export default class RFB extends EventTargetMixin {
+        this._lastDataEncoding = null;
@@ -300,0 +302,3 @@ export default class RFB extends EventTargetMixin {
+        this._remoteResizePixelRatio = 1;
+        this._remoteResizeDebounce = 250;
+        this._enableH264 = false;
@@ -366,0 +371,34 @@ export default class RFB extends EventTargetMixin {
+    get remoteResizePixelRatio() { return this._remoteResizePixelRatio; }
+    set remoteResizePixelRatio(ratio) {
+        if (typeof ratio !== 'number' || !Number.isFinite(ratio) || ratio < 1 || ratio > 2) {
+            Log.Error("remoteResizePixelRatio must be a number between 1 and 2");
+            return;
+        }
+        this._remoteResizePixelRatio = ratio;
+        this._requestRemoteResize();
+    }
+
+    get remoteResizeDebounce() { return this._remoteResizeDebounce; }
+    set remoteResizeDebounce(delay) {
+        if (!Number.isInteger(delay) || delay < 100 || delay > 2000) {
+            Log.Error("remoteResizeDebounce must be an integer between 100 and 2000");
+            return;
+        }
+        this._remoteResizeDebounce = delay;
+    }
+
+    get enableH264() { return this._enableH264; }
+    set enableH264(enabled) {
+        if (typeof enabled !== 'boolean') {
+            Log.Error("enableH264 must be a boolean");
+            return;
+        }
+        if (this._enableH264 === enabled) {
+            return;
+        }
+        this._enableH264 = enabled;
+        if (this._rfbConnectionState === 'connected') {
+            this._sendEncodings();
+        }
+    }
+
@@ -506,3 +545,12 @@ export default class RFB extends EventTargetMixin {
             this._clipboardText = text;
             RFB.messages.extendedClipboardNotify(this._sock, [extendedClipboardFormatText]);
-        } else {
+            if (this._clipboardServerCapabilitiesActions[extendedClipboardActionProvide]) {
+                RFB.messages.extendedClipboardProvide(this._sock, [extendedClipboardFormatText], [this._clipboardText]);
+                this.dispatchEvent(new CustomEvent("clipboardpastecomplete", { detail: {} }));
+            }
+        } else if (this._clipboardServerCapabilitiesFormats[extendedClipboardFormatText] &&
+                   this._clipboardServerCapabilitiesActions[extendedClipboardActionProvide]) {
+            this._clipboardText = text;
+            RFB.messages.extendedClipboardProvide(this._sock, [extendedClipboardFormatText], [this._clipboardText]);
+            this.dispatchEvent(new CustomEvent("clipboardpastecomplete", { detail: {} }));
+        } else {
@@ -530,6 +575,7 @@ export default class RFB extends EventTargetMixin {
             }

             RFB.messages.clientCutText(this._sock, data);
+            this.dispatchEvent(new CustomEvent("clipboardpastecomplete", { detail: {} }));
         }
     }

@@ -788 +834 @@ export default class RFB extends EventTargetMixin {
-    _requestRemoteResize() {
+    _requestRemoteResize(immediate = false) {
@@ -799,2 +845,6 @@ export default class RFB extends EventTargetMixin {
-        // Rate limit to one pending resize at a time
-        if (this._pendingRemoteResize) {
+        if (!immediate) {
+            clearTimeout(this._resizeTimeout);
+            this._resizeTimeout = setTimeout(() => {
+                this._resizeTimeout = null;
+                this._requestRemoteResize(true);
+            }, this._remoteResizeDebounce);
@@ -804,5 +854,2 @@ export default class RFB extends EventTargetMixin {
-        // And no more than once every 100ms
-        if ((Date.now() - this._lastResize) < 100) {
-            clearTimeout(this._resizeTimeout);
-            this._resizeTimeout = setTimeout(this._requestRemoteResize.bind(this),
-                                             100 - (Date.now() - this._lastResize));
+        // Rate limit to one pending resize at a time
+        if (this._pendingRemoteResize) {
@@ -811 +857,0 @@ export default class RFB extends EventTargetMixin {
-        this._resizeTimeout = null;
@@ -833 +879,4 @@ export default class RFB extends EventTargetMixin {
-        return { w: r.width, h: r.height };
+        return {
+            w: r.width * this._remoteResizePixelRatio,
+            h: r.height * this._remoteResizePixelRatio,
+        };
@@ -2235 +2284 @@ export default class RFB extends EventTargetMixin {
-            if (supportsWebCodecsH264Decode) {
+            if (this._enableH264 && supportsWebCodecsH264Decode) {
@@ -2399,6 +2448,7 @@ export default class RFB extends EventTargetMixin {

                     if (formats & extendedClipboardFormatText) {
                         RFB.messages.extendedClipboardProvide(this._sock, [extendedClipboardFormatText], [this._clipboardText]);
+                        this.dispatchEvent(new CustomEvent("clipboardpastecomplete", { detail: {} }));
                     }
                 }

@@ -2661,0 +2712 @@ export default class RFB extends EventTargetMixin {
+        this.dispatchEvent(new CustomEvent("framebufferupdate", { detail: {} }));
@@ -2983,0 +3036,9 @@ export default class RFB extends EventTargetMixin {
+        if (this._lastDataEncoding !== this._FBU.encoding) {
+            this._lastDataEncoding = this._FBU.encoding;
+            this.dispatchEvent(new CustomEvent("encodingchange", {
+                detail: {
+                    encoding: this._FBU.encoding,
+                    name: encodingName(this._FBU.encoding),
+                },
+            }));
+        }
