# SPDX-FileCopyrightText: 2025 Weibo, Inc.
#
# SPDX-License-Identifier: Apache-2.0

image: registry.api.weibo.com/ci/ci-runner:extend-fix-utf8-snapshot

variables:
  K8S_NAMESPACE: ci-deploy-video
  MAIL_TO: weibo_rd_video@staff.sina.com.cn
  PAAS_NAMESPACE: wb-plat-ide
  MASTER_BRANCH: main
stages:
  - validation
  - package
  - production
  - auto-merge-request

# WeAgent MR Validation Job
# validate-wegent-mr:
#   stage: validation
#   image: registry.api.weibo.com/ci/ci-runner:extend
#   script:
#     - |
#       # Check if code comes from "wegent"
#       if [[ "$CI_COMMIT_REF_NAME" =~ ^wegent ]]; then
#         exit 1
#       fi
#   rules:
#     - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop"'
#       when: always

wegent-runtime:
  stage: package
  retry: 1
  variables:
    SERVICE: wegent-runtime
    DOCKERFILE_PATH: wecode/docker/base
  script:
    - build_image
  when: manual
  rules:
    - when: manual

wgent-shell:
  stage: package
  retry: 1
  variables:
    SERVICE: wgent-shell
    DOCKERFILE_PATH: wecode/docker/wgent_shell
  script:
    - build_image
  when: manual
  rules:
    - when: manual

wegent-executor-manager:
  stage: package
  retry: 1
  variables:
    SERVICE: wegent-executor-manager
    DOCKERFILE_PATH: wecode/docker/executor_manager
  script:
    - build_image
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "develop"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - executor_manager/**/*
        - wecode/docker/executor_manager/**/*
        - shared/**/*
    - changes:
      - executor_manager/**/*
      - wecode/docker/executor_manager/**/*
      - shared/**/*

trigger_update_executor_manager:
    stage: production
    variables:
        APP: wegent-executor-manager-web;simulation-wegent-executor-manager-web
        SERVICE: wegent-executor-manager
        IMAGE_NAME: wegent-executor-manager-web
        UPSTREAM_CI_PROJECT_ID: $CI_PROJECT_ID
        UPSTREAM_CI_PIPELINE_ID: $CI_PIPELINE_ID
        UPSTREAM_CI_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
        UPSTREAM_CI_COMMIT_SHA: $CI_COMMIT_SHA
        MASTER_BRANCH: master
    trigger:
        project: weibo_rd/cloudnative/aigc_gitops_manifest
        branch: master
        strategy: depend
    rules:
      - if: '$CI_COMMIT_BRANCH == "main"'
        changes:
          - executor_manager/**/*
          - wecode/docker/executor_manager/**/*
          - shared/**/*

trigger_update_simulation_executor_manager:
    stage: production
    variables:
        APP: simulation-wegent-executor-manager-web
        SERVICE: wegent-executor-manager
        IMAGE_NAME: wegent-executor-manager-web
        MASTER_BRANCH: master
        UPSTREAM_CI_PROJECT_ID: $CI_PROJECT_ID
        UPSTREAM_CI_PIPELINE_ID: $CI_PIPELINE_ID
        UPSTREAM_CI_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
        UPSTREAM_CI_COMMIT_SHA: $CI_COMMIT_SHA
    trigger:
        project: weibo_rd/cloudnative/aigc_gitops_manifest
        branch: master
        strategy: depend
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME == "develop" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main"'
        changes:
          - executor_manager/**/*
          - wecode/docker/executor_manager/**/*
          - shared/**/*

wegent-executor:
  stage: package
  retry: 1
  variables:
    SERVICE: wegent-executor
    DOCKERFILE_PATH: wecode/docker/executor
  script:
    - build_image
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "develop"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - executor/**/*
        - wecode/docker/executor/**/*
        - shared/**/*
    - changes:
      - executor/**/*
      - wecode/docker/executor/**/*
      - shared/**/*

trigger_update_executor:
    stage: production
    variables:
        APP: wegent-executor-manager-web;simulation-wegent-executor-manager-web
        SERVICE: wegent-executor
        IMAGE_NAME: wegent-executor
        UPSTREAM_CI_PROJECT_ID: $CI_PROJECT_ID
        UPSTREAM_CI_PIPELINE_ID: $CI_PIPELINE_ID
        UPSTREAM_CI_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
        UPSTREAM_CI_COMMIT_SHA: $CI_COMMIT_SHA
        MASTER_BRANCH: master
    trigger:
        project: weibo_rd/cloudnative/aigc_gitops_manifest
        branch: master
        strategy: depend
    rules:
      - if: '$CI_COMMIT_BRANCH == "main"'
        changes:
          - executor/**/*
          - wecode/docker/executor/**/*
          - shared/**/*

trigger_update_simulation_executor:
    stage: production
    variables:
        APP: simulation-wegent-executor-manager-web
        SERVICE: wegent-executor
        IMAGE_NAME: wegent-executor
        MASTER_BRANCH: master
        UPSTREAM_CI_PROJECT_ID: $CI_PROJECT_ID
        UPSTREAM_CI_PIPELINE_ID: $CI_PIPELINE_ID
        UPSTREAM_CI_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
        UPSTREAM_CI_COMMIT_SHA: $CI_COMMIT_SHA
    trigger:
        project: weibo_rd/cloudnative/aigc_gitops_manifest
        branch: master
        strategy: depend
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME == "develop" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main"'
        changes:
          - executor/**/*
          - wecode/docker/executor/**/*
          - shared/**/*

wegent-backend:
  stage: package
  retry: 1
  variables:
    SERVICE: wegent-backend-web
    DOCKERFILE_PATH: wecode/docker/backend
  script:
    - build_image
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "develop"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - backend/**/*
        - wecode/docker/backend/**/*
        - shared/**/*
        - knowledge_engine/**/*
    - changes:
      - backend/**/*
      - wecode/docker/backend/**/*
      - shared/**/*
      - knowledge_engine/**/*

trigger_update_backend_web:
    stage: production
    variables:
        APP: wegent-backend-web;simulation-wegent-backend-web;beta-wegent-backend-web;outer-wegent-backend-web
        SERVICE: wegent-backend-web
        IMAGE_NAME: wegent-backend-web
        UPSTREAM_CI_PROJECT_ID: $CI_PROJECT_ID
        UPSTREAM_CI_PIPELINE_ID: $CI_PIPELINE_ID
        UPSTREAM_CI_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
        UPSTREAM_CI_COMMIT_SHA: $CI_COMMIT_SHA
        MASTER_BRANCH: master
    trigger:
        project: weibo_rd/cloudnative/aigc_gitops_manifest
        branch: master
        strategy: depend
    rules:
      - if: '$CI_COMMIT_BRANCH == "main"'
        changes:
          - backend/**/*
          - wecode/docker/backend/**/*
          - shared/**/*
          - knowledge_engine/**/*

wegent-chat-shell:
  stage: package
  retry: 1
  variables:
    SERVICE: wegent-chat-shell-web
    DOCKERFILE_PATH: wecode/docker/chat_shell
  script:
    - build_image
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "develop"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - chat_shell/**/*
        - wecode/docker/chat_shell/**/*
        - shared/**/*
    - changes:
      - chat_shell/**/*
      - wecode/docker/chat_shell/**/*
      - shared/**/*

trigger_update_chat_shell:
    stage: production
    variables:
        APP: wegent-chat-shell-web;simulation-wegent-chat-shell-web;beta-wegent-chat-shell-web;outer-wegent-chat-shell-web
        SERVICE: wegent-chat-shell-web
        IMAGE_NAME: wegent-chat-shell-web
        UPSTREAM_CI_PROJECT_ID: $CI_PROJECT_ID
        UPSTREAM_CI_PIPELINE_ID: $CI_PIPELINE_ID
        UPSTREAM_CI_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
        UPSTREAM_CI_COMMIT_SHA: $CI_COMMIT_SHA
        MASTER_BRANCH: master
    trigger:
        project: weibo_rd/cloudnative/aigc_gitops_manifest
        branch: master
        strategy: depend
    rules:
      - if: '$CI_COMMIT_BRANCH == "main"'
        changes:
          - chat_shell/**/*
          - wecode/docker/chat_shell/**/*
          - shared/**/*

trigger_update_simulation_chat_shell:
    stage: production
    variables:
        APP: simulation-wegent-chat-shell-web
        SERVICE: wegent-chat-shell-web
        IMAGE_NAME: wegent-chat-shell-web
        MASTER_BRANCH: master
        UPSTREAM_CI_PROJECT_ID: $CI_PROJECT_ID
        UPSTREAM_CI_PIPELINE_ID: $CI_PIPELINE_ID
        UPSTREAM_CI_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
        UPSTREAM_CI_COMMIT_SHA: $CI_COMMIT_SHA
        CD_AUTO_PRODUCTION: 1
    trigger:
        project: weibo_rd/cloudnative/aigc_gitops_manifest
        branch: master
        strategy: depend
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME == "develop" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main"'
        changes:
          - chat_shell/**/*
          - wecode/docker/chat_shell/**/*
          - shared/**/*

wegent-web:
  stage: package
  retry: 1
  variables:
    SERVICE: wegent-web
    DOCKERFILE_PATH: wecode/docker/frontend
  script:
    - build_image
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "develop"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - frontend/**/*
        - wecode/docker/frontend/**/*
    - changes:
      - frontend/**/*
      - wecode/docker/frontend/**/*

trigger_update_front_web:
    stage: production
    variables:
        APP: wegent-web;simulation-wegent-web;beta-wegent-web
        SERVICE: wegent-web
        IMAGE_NAME: wegent-web
        MASTER_BRANCH: master
        UPSTREAM_CI_PROJECT_ID: $CI_PROJECT_ID
        UPSTREAM_CI_PIPELINE_ID: $CI_PIPELINE_ID
        UPSTREAM_CI_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
        UPSTREAM_CI_COMMIT_SHA: $CI_COMMIT_SHA
    trigger:
        project: weibo_rd/cloudnative/aigc_gitops_manifest
        branch: master
        strategy: depend
    rules:
      - if: '$CI_COMMIT_BRANCH == "main"'
        changes:
          - frontend/**/*
          - wecode/docker/frontend/**/*

trigger_update_simulation_web:
    stage: production
    variables:
        APP: simulation-wegent-web
        SERVICE: wegent-web
        IMAGE_NAME: wegent-web
        MASTER_BRANCH: master
        UPSTREAM_CI_PROJECT_ID: $CI_PROJECT_ID
        UPSTREAM_CI_PIPELINE_ID: $CI_PIPELINE_ID
        UPSTREAM_CI_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
        UPSTREAM_CI_COMMIT_SHA: $CI_COMMIT_SHA
        CD_AUTO_PRODUCTION: 1
    trigger:
        project: weibo_rd/cloudnative/aigc_gitops_manifest
        branch: master
        strategy: depend
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME == "develop" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main"'
        changes:
          - frontend/**/*
          - wecode/docker/frontend/**/*

trigger_update_simulation_backend_web:
    stage: production
    variables:
        APP: simulation-wegent-backend-web
        SERVICE: wegent-backend-web
        IMAGE_NAME: wegent-backend-web
        MASTER_BRANCH: master
        UPSTREAM_CI_PROJECT_ID: $CI_PROJECT_ID
        UPSTREAM_CI_PIPELINE_ID: $CI_PIPELINE_ID
        UPSTREAM_CI_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
        UPSTREAM_CI_COMMIT_SHA: $CI_COMMIT_SHA
        CD_AUTO_PRODUCTION: 1
    trigger:
        project: weibo_rd/cloudnative/aigc_gitops_manifest
        branch: master
        strategy: depend
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME == "develop" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main"'
        changes:
          - backend/**/*
          - wecode/docker/backend/**/*

wegent-outer-web:
  stage: package
  retry: 1
  variables:
    SERVICE:  outer-wegent-web
    DOCKERFILE_PATH: wecode/docker/frontend_outer
  script:
    - build_image
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "develop"'
      when: never
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - frontend/**/*
        - wecode/docker/frontend_outer/**/*
    - changes:
      - frontend/**/*
      - wecode/docker/frontend_outer/**/*

trigger_update_outer_front_web:
    stage: production
    variables:
        APP: outer-wegent-web
        SERVICE: outer-wegent-web
        IMAGE_NAME: outer-wegent-web
        MASTER_BRANCH: master
        UPSTREAM_CI_PROJECT_ID: $CI_PROJECT_ID
        UPSTREAM_CI_PIPELINE_ID: $CI_PIPELINE_ID
        UPSTREAM_CI_COMMIT_REF_NAME: $CI_COMMIT_REF_NAME
        UPSTREAM_CI_COMMIT_SHA: $CI_COMMIT_SHA
    trigger:
        project: weibo_rd/cloudnative/aigc_gitops_manifest
        branch: master
        strategy: depend
    rules:
      - if: '$CI_COMMIT_BRANCH == "main"'
        changes:
          - frontend/**/*
          - wecode/docker/frontend/**/*

wegent-ops-web:
  stage: package
  retry: 1
  variables:
    SERVICE: wegent-ops-web
    DOCKERFILE_PATH: wecode/docker/frontend_ops
  script:
    - build_image
  when:
    manual
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "develop"'
      when: never
    - changes:
      - frontend/**/*
      - wecode/docker/frontend_ops/**/*
      when: manual
# Auto create MR from develop to main when branch merged to develop
auto-create-develop-to-main-mr:
  stage: auto-merge-request
  image: registry.api.weibo.com/ci/ci-runner:extend
  script:
    - |
      #!/bin/bash
      set -e

      echo "Checking if MR from develop to main already exists..."

      # GitLab API endpoint
      GITLAB_API="https://git.intra.weibo.com/api/v4"
      PROJECT_ID="${CI_PROJECT_ID}"

      # Get existing MRs from develop to main with error handling
      HTTP_CODE=$(curl -w "%{http_code}" --silent --output /tmp/check_mr.json \
        --header "PRIVATE-TOKEN: ${WECODER_TOKEN}" \
        "${GITLAB_API}/projects/${PROJECT_ID}/merge_requests?state=opened&source_branch=develop&target_branch=main")

      if [ "$HTTP_CODE" -ne 200 ]; then
        echo "Error: Failed to check existing MRs. HTTP status code: $HTTP_CODE"
        echo "Response: $(cat /tmp/check_mr.json)"
        exit 1
      fi

      EXISTING_MR=$(jq '.[0].iid' /tmp/check_mr.json)

      # Improved validation: check if MR is a valid number
      if [[ "$EXISTING_MR" != "null" ]] && [[ "$EXISTING_MR" =~ ^[0-9]+$ ]]; then
        echo "MR from develop to main already exists (MR !${EXISTING_MR}), skipping creation."
        exit 0
      fi

      echo "No existing MR found, creating new MR from develop to main..."

      # Create MR from develop to main with error handling
      HTTP_CODE=$(curl -w "%{http_code}" --silent --output /tmp/create_mr.json \
        --request POST --header "PRIVATE-TOKEN: ${WECODER_TOKEN}" \
        --data "source_branch=develop" \
        --data "target_branch=main" \
        --data "title=Auto MR: Merge develop to main" \
        --data "description=Automatically created MR to merge develop branch into main branch." \
        "${GITLAB_API}/projects/${PROJECT_ID}/merge_requests")

      if [ "$HTTP_CODE" -ne 201 ]; then
        echo "Error: Failed to create MR. HTTP status code: $HTTP_CODE"
        echo "Response: $(cat /tmp/create_mr.json | jq '.')"
        exit 1
      fi

      MR_IID=$(jq -r '.iid' /tmp/create_mr.json)
      MR_URL=$(jq -r '.web_url' /tmp/create_mr.json)

      if [[ "$MR_IID" =~ ^[0-9]+$ ]]; then
        echo "Successfully created MR !${MR_IID}"
        echo "MR URL: ${MR_URL}"
      else
        echo "Error: Failed to extract MR IID from response."
        echo "Response: $(cat /tmp/create_mr.json | jq '.')"
        exit 1
      fi
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "develop"'
      when: on_success
