- insert:
    - id: wework-user-context
      name: '@wework/dsh-user-context'
      inject:
        - tools
        - web
      config:
        baseUrl: !!js process.env.WEWORK_HARNESS_CONTEXT_BASE_URL
- id: web
  config:
    searchProvider: deepseek-official
    fetchProvider: wework-loopback
